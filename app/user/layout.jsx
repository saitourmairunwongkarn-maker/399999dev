export const metadata = {
  title: 'Register - Devbaanban',
  description: 'Create a new account',
}

export default function RegisterLayout({ children }) {
  return <div className="container mx-auto p-8">{children}</div>
}