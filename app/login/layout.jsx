export const metadata = {
  title: 'Login - Devbaanban',
  description: 'Login to your account',
}

export default function LoginLayout({ children }) {
  return <div className="container mx-auto p-8">{children}</div>
}